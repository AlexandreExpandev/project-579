import { Router } from 'express';

const router = Router();

// Placeholder for public routes like authentication, registration, etc.
// Example: router.use('/auth', authRoutes);

export default router;
